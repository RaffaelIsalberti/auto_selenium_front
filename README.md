Pasta venv não foi inserida no projeto - 
criar ambiente virtual ao testar com o comando no terminal --> python -m venv venv
apos criar, lembrar de executar no terminal o comando---> pip install selenium
